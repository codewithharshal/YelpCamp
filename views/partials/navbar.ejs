<nav class="navbar sticky-top navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <!-- Campgrounds on the left -->
    <a class="navbar-brand me-auto" href="/">Campgrounds</a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div
      class="collapse navbar-collapse justify-content-center"
      id="navbarNavAltMarkup"
    >
      <!-- Centered Navigation Links with Gap -->
      <div class="navbar-nav d-flex gap-3">
        <a class="nav-link" href="/">Home</a>
        <% if(currentUser) { %>
        <a class="nav-link" href="/campgrounds">All Camps</a>
        <a class="nav-link" href="/campgrounds/new">Add Camps</a>
        <% } %>
      </div>
    </div>

    <!-- Login/Register on the right -->
    <div class="navbar-nav ms-auto">
      <% if (!currentUser) { %>
      <a class="nav-link" href="/login">Login</a>
      <a class="nav-link" href="/register">Register</a>
      <% } else { %>
      <a class="nav-link" href="/logout">Logout</a>
      <% } %>
    </div>
  </div>
</nav>
